# Telemetry Protocol

Protocol workspace for compact binary telemetry packet definitions, examples,
and test vectors.

Do not use JSON or text packets over RF for v1 telemetry. Keep packet formats
small, deterministic, and practical for STM32L433 firmware and XBee transport.
